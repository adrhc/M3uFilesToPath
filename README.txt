The Ctrl+Alt+E only loads the metadata from the Media Library and not from the file.

To load metadata from the file:
Uncheck "Use Library title info for Playlist Item Formatting" first in: 
Winamp > Prefs (Ctrl+P) > Media Library > Local Media > Options tab. 
